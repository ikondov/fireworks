========================
Using the Dataflow Tasks
========================

This group includes custom firetasks to realize dataflow between fireworks. The 
input data and output data are stored in the Firework **spec** and passed to the 
subsequent firetasks and fireworks via FWAction objects. The module includes:

* SingleTask
* ForeachTask
* JoinDictTask
* JoinListTask
* ImportDataTask

SingleTask
==========

The SingleTask is similar to PyTask, i.e. it allows to integrate a Python 
function into the firework. It is very useful if parts of existing python 
scripts have to be reused at many places and executed as separate jobs. 

Required parameters
-------------------

* **function**: *(str)* - A name of a python function. If the function is part of a module then the name specification is something like *module.function*.

Optional parameters
-------------------

* **inputs**: *([str])* - A list of keys in the **spec** from which the positional arguments will be read and then passed to the function.
* **outputs**: *([str])* - A list of keys under which the outputs will be stored in the **spec** and passed to the subsequent fireworks. If the list is empty (*[]*) or not specified (*None*) then the output of the function will not be stored. If **chunk_number** is defined then this must be a name of a list in the **spec**, i.e. a *str*.
* **chunk_number**: *(int)* - This is a serial number of the task if it is a member of a group, for example generated by a *ForeachTask*. If *chunk_number* is not *None* the output will be merged with the output of the other firetasks in the group in a list named *outputs*.

Example
-------

Here is an example of using the *SingleTask*::

    fws:
    - fw_id: 1
      name: SingleTask demo
      spec:
        _tasks:
        - _fw_name: SingleTask
          function: mytest.test_func
          inputs: data
          outputs: passed data
        data: 'Hello world'
    - fw_id: 2
      spec:
        _tasks:
        - _fw_name: ScriptTask
          script:
          - echo "Data passed by the previous firework:"
        - _fw_name: SingleTask
          function: mymodule.test_func
          inputs: passed data
    links:
      '1':
      - 2
      '2': []

Listing of the file *mymodule.py* (its folder must be found in $PYTHONPATH)::

    def test_func(arg, optional=None):
        print(arg)
        return arg




ForeachTask
===========

The purpose of ForeachTask is to dynamically branch the workflow between this FW and its children by inserting a parallel section of children FWs using *FWAction* detour. The number of the parallel FWs is determined by the length of the list specified by the *split* parameter or the optional *number of chunks* parameter. Each child FW contains a *SingleTask* which processes one element (or a chunk) from this list. The return value is passed to the spec of the FW right after the detour using a push method, i.e. the values of all parallel SingleTasks are collected in a list named by the ‘output’ argument. However, the ordering of elements in the resulting *output* list can be different from that in the original *split* list.


Required parameters
-------------------
* **function**: *(str)* - A name of a python function. If the function is part of a module then the name specification is something like *module.function*.
* **inputs**: *([str])* - A list of keys from the **spec** from which the positional arguments will be read and then passed to the function.
* **split**: *(str)* - A key from the **spec** specifying the data to be distributed to several new *SingleTasks*. This parameter must be element of the *inputs* list and the field *spec[split]* must be a list.


Optional parameters
-------------------
* **outputs**: -  A list of keys under which the outputs will be stored in the **spec** and passed to the subsequent fireworks. If the list is empty (*[]*) or not specified (*None*) then the output of the function will not be stored.
* **number of chunks**: *(int)* - The number of chunks into which the list *spec[split]* will be split. Default number of chunks is the number of elements in *spec[split]*

Example
-------

The following example demonstrates the use of *ForeachTask*::

    fws:
    - fw_id: 1
      spec:
        _tasks:
        - _fw_name: ForeachTask
          inputs: array
          function: mymodule.test_func
          outputs: new array
          split: array
        array:
        - 1
        - five
        - number: seven
    - fw_id: 2
      spec:
        _tasks:
        - _fw_name: ScriptTask
          script:
          - echo "The combined result of all parent fireworks:"
        - _fw_name: SingleTask
          inputs:
          - new array
          - optional
          function: mymodule.test_func
          outputs: outputs
        optional: blah
    links:
      '1':
      - 2
      '2': []

The *inputs* list contains only the name *array* of one list. The three elements of *array* are of *int*, a *str* and a *dict* types, respectively. In the first FW the workflow is extended with three additional FWs each processing one element of *array*. After all these three FWs are completed their individual results are reassembled in the data element *new array* in the last FW and printed on the screen.


JoinDictTask
============

Required parameters
-------------------
Optional parameters
-------------------
Example
-------

JoinListTask
============

Required parameters
-------------------
Optional parameters
-------------------
Example
-------

ImportDataTask
==============

Required parameters
-------------------
Optional parameters
-------------------
Example
-------

